{
  "dependencies": {
    "axios": "^0.21.1",
    "tsconfig": "^7.0.0"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
